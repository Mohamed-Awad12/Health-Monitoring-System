# Optional. Leave empty to auto-resolve to current host (useful for LAN access in dev).
VITE_API_URL=
# Optional. Leave empty to auto-resolve to current host (useful for LAN access in dev).
VITE_SOCKET_URL=
